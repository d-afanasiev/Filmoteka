import "./transitToMain.js";

// // Spinner usage:
// import Spinner from './utils/spinner';

// const headerSpinner = new Spinner({ element: 'header' }); //specify element by CSS selector and use spinner with default settings

// //or chacge anything
// const footerSpinner = new Spinner({
//   element: 'footer',
//   message: 'Loading...',
//   svgColor: '#0f0',
//   messageColor: '#00f',
//   backgroundColor: 'rgba(100,255,100,0.5)',
//   svgSize: '80px',
//   messageFontSize: '30px',
// });

// //setTimeout foo just for example
// setTimeout(() => {
//   headerSpinner.show();//call .show() method parrallel to your async foo
// }, 1);
// setTimeout(() => {
//   footerSpinner.show();
// }, 1);

// setTimeout(() => {
//   headerSpinner.hide();//call .hide when rendering is complete
// }, 2000);

// setTimeout(() => {
//   footerSpinner.hide();
// }, 3000);
